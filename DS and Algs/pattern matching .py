print("plz enter a string")
text = input()
print("plz enter a pattern to be matched")
pattern = input()

index = text.find(pattern)
#while index > -1:
print(index+1)
   # index = text.find(pattern, index+1)
